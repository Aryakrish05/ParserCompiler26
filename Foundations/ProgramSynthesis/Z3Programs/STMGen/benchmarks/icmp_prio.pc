int parse () {
     Extract(icmp_typeCode_0,16);
     if((icmp_typeCode_0&65024)==33280)
     {
         Extract(data_f16_2,1);
         return ACCEPT;
     }
     else
     if((icmp_typeCode_0&64512)==33792)
     {
         Extract(data_f16_2,1);
         return ACCEPT;
     }
     else
     if((icmp_typeCode_0&63488)==34816)
     {
         Extract(data_f16_2,1);
         return ACCEPT;
     }
     else
     {
         return ACCEPT;
     }
 }
