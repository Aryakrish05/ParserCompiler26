int parse () {
     Extract(h_v_0,2);
     if(h_v_0==0)
     {
         Extract(p_d_1,1);
         return ACCEPT;
     }
     if(h_v_0==1)
     {
         Extract(p_d_1,1);
         return ACCEPT;
     }
     Extract(p_d_1,1);
     return ACCEPT;
 }
