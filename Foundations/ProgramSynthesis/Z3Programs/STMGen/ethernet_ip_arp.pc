
int parse(){
    Extract(ethernet_src_dst,48);
    Extract(ethernet_type,16);
    if(ethernet_type==2048){
        Extract(ipv4,32);
    }
    else if(ethernet_type==2054){
        Extract(arp,32);
    }
    return ACCEPT;
}